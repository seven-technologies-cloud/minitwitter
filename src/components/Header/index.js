import { HeaderContainer, LogoText } from "./styles";

export function Header() {
  return (
    <HeaderContainer>
      <LogoText>
        p<span>o</span>sterr<span>.</span>
      </LogoText>
    </HeaderContainer>
  );
}
